we've got a question component with slider. it works quite nice.

<question id="python-wichtig" type="number" minValue="1" maxValue="10" step="0.1">
1 = irrelevant · 10 = würde ich einsetzen
</question>

however the way we have to add 1 = irrelevant - 10 = i'd use that is not nice. lets add an optional child component or attribute so i can label both extremes of the slider. they should appear beneath the slider at either end.

---
